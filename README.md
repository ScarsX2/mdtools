# mdtools

Static assets for `mdtools.pl` — single-file HTML tools + JSON data.

## Files

- `index.html` — main app (single-file, static, deployable anywhere)
- `data.json` — data source, fetched by the app at runtime

## Local dev

Serve the folder over HTTP (any static server). App fetches `data.json` from the raw URL of this repo.
